To run the code with R Studio:

-Download the directory IRDM_DATA_STOCKS 
-Change the variable mypath to reflect the path where the IRDM_DATA_STOCKS directory is.
-Select all the code and hit the run button.